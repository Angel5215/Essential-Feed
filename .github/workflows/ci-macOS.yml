name: ci-macOS

on:
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: macos-26
    timeout-minutes: 10

    steps:
      - name: Fetch Sources
        uses: actions/checkout@v6

      - name: Setup Xcode
        run: |
          bin/setup-xcode.sh

      - name: Build and test
        env:
          SCHEME: "CI-macOS"
          PLATFORM_DESTINATION: "platform=macOS"
          TARGET_SDK: "macosx"
        run: |
          bin/build-and-test.sh
